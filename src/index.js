import React from "react";  
import ReactDOM from "react-dom/client";
import App  from "./App";
const htmlCont = document.getElementById("root");
const rootObj = ReactDOM.createRoot(htmlCont);
rootObj.render(
<App/>
);

